spring:
  r2dbc:
    url:
    username:
    password:
  liquibase:
    driver-class-name:
    url:
    user:
    password:
    contexts:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  cloud:
    consul:
      enabled: false
      config:
        enabled: false
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:9999/.well-known/jwks.json

testcontainers:
  postgres:
    tag: 17.5-alpine3.22