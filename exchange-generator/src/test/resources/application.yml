spring:
  cloud:
    consul:
      enabled: false
      config:
        enabled: false
  security:
    oauth2:
      client:
        registration:
          bank-services-cc:
            provider: keycloak
            client-id: test
            client-secret: test
            authorization-grant-type: client_credentials
        provider:
          keycloak:
            token-uri: http://localhost:9999/token

application:
  scheduling:
    enable: false